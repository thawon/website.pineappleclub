define(
    ["app"],
    function (app) {
        "use strict";

        app.controller("HomeController",
            ["$scope",
            function ($scope) {
                
            }
        ]);
    });