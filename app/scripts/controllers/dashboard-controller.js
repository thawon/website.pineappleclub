define(
    ["app"],
    function (app) {
        "use strict";

        app.controller("DashboardController",
            ["$scope",
            function ($scope) {
                
            }
        ]);
    });